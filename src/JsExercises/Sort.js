//Create a function that arranges the numbers from least to greatest.
const nums = [11,13,70,23,4,6,2,10];

const orderedNums = nums.sort((a,b) => a - b);

console.log(orderedNums);

//Create a function that arranges the words in alphabetical order.

const words = ['apple', 'banana', 'pineapple', 'orange', 'grape'];

const orderedWords = words.sort();

console.log(orderedWords);