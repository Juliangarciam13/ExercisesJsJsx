//Create a function that takes length width and finds the perimeter of a rectangle.

const findPerimeter = (length,width) => {
    return 2 * (length + width);
}

console.log(findPerimeter(6,7));
console.log(findPerimeter(20,10));